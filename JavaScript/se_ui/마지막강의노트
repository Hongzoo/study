console.log() 는 개발자용으로 사용하므로 최종코드에서는 제거.
ie7~8 에서는 콘솔오류가 난다.

다른 사람들이 작성한 주석을 많이 보라. 어떤식으로 기록하는지

호이스팅 : 코드 실행되는 순서가 무너짐.

즉시실행함수 : 문서 불러온 후 1번만 실행되고 다시 실행할 수 가 없다.

extend 는 1단계 뎁스의 객체만 머지가 가능하다
첫번째 파라미터로 {} 빈객체를 넣어야 알수없는 오류를 방지할 수 있다.

extend로 안되는 것은 UTIL.def 로 해결~?

$.fn.메서드명 은 뭔가 반복을 줄여준대..


에미터, 외부 콜백함수
